import { StyleSheet } from "react-native";
import { colors } from "@/theme";

export default StyleSheet.create({
	btn: { flexDirection: "row", alignItems: "center", gap: 10 },
	label: { fontSize: 20, fontWeight: "bold", color: colors.primary },
});
